# Mock mode is free and requires no external credentials.
SYCRELY_INFERENCE_MODE=mock

# Live mode requires all three values below.
OPENROUTER_API_KEY=
OPENROUTER_MODEL=
SYCRELY_ALLOWED_MODELS=

# Optional attribution header sent to OpenRouter.
SYCRELY_SITE_URL=http://localhost:3000
